# Thesis AI Web (Marketing)

Marketing site for Thesis AI.

## Run locally
- `pnpm -C apps/web install`
- `pnpm -C apps/web dev`

The site runs on `http://localhost:3001`.

## Build
- `pnpm -C apps/web build`
- `pnpm -C apps/web start`
